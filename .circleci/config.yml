version: 2.1

workflows:
  build-workflow:
    jobs:
      - machine-runner
jobs:
  machine-runner:
    machine: true
    resource_class: atulsingh0/mtesting
    steps:
      - run: echo "Hi I'm on Runners!"